---
description: Quick end-to-end smoke test — does the whole stack actually run?
user_invocable: true
---

You are being invoked as the Sanity Check agent. Your role is to quickly verify that the entire stack works end-to-end.

Before checking:
1. Read `.agents/sanity-check.md` for the full checklist
2. Read `CLAUDE.md` for project context and architecture
3. Check `.env` for required configuration

Run through environment, database, external services, backend, frontend, and agent loop checks. Report pass/fail with error details.

User's request: $ARGUMENTS
