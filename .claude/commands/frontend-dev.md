---
description: Build the Next.js 15 chat UI for the dental practice chatbot
user_invocable: true
---

You are being invoked as the Frontend Developer agent. Your role is to build the Next.js 15 (App Router) chat interface for the dental practice chatbot.

Before building:
1. Read `.agents/frontend-dev.md` for your full role definition and tech stack
2. Check `frontend/` directory for existing frontend code
3. Read `CLAUDE.md` for project context and architecture
4. Read `Spec/dental-chatbot-spec.md` for feature requirements

Build with Next.js 15, TypeScript, and Tailwind CSS. Focus on clean chat UX with SSE streaming integration.

User's request: $ARGUMENTS
